/*
#include "MesLibrairies/to_string.h"

#include <string>
using namespace std;

string to_string(const Date& date) {
    return to_string(date.jour) + '/' + to_string(date.mois) + '/' + to_string(date.annee);
}*/

