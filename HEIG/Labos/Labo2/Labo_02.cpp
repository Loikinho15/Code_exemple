//
// Created by loicd on 19.09.2024.
//
#include <istream>
#include <iostream>
#include <cmath>
#include <iomanip>
#include <cstdio>
using namespace std;
/*
int main() {
    const int speed_on_road = 5; // en km/h
    const int speed_off_road = 2; //en km/h
    const int tot_road_lenght = 10; // en km

    float lenght_on_road = 0;

    while (true) {
        printf("Quelle longueur souhaitez-vous parcourir sur la route : \n*****Veuillez écrir \"-1\" pour quitter le programme*****\n");
        cin >> lenght_on_road;

        if (lenght_on_road >= 0) {
            //Calcule du temps en heure pour la première distance en Km
            float time_to_travel_road = lenght_on_road / speed_on_road;

            //Pythagore pour trouver la longueur entre le robot et la colis après avoir parcouru la distance sur la route
            float lenght_off_road = sqrt(pow(3,2) + pow(tot_road_lenght-lenght_on_road,2));
            //Caclul du temps pour aller jusqu'au colis depuis la route
            float time_to_travel_off_road = lenght_off_road/speed_off_road;

            //Temps total
            float total_time = time_to_travel_road + time_to_travel_off_road;

            printf("Temps total : %.2f\n\n", total_time);
        } else break;
    }
}*/